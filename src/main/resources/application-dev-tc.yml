spring:
  datasource:
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
  jpa:
    hibernate:
      ddl-auto: validate
  testcontainers:
    enabled: true
  sql:
    init:
      mode: never

security:
  mock: true
